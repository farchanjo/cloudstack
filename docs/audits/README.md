# OVN Fork Audit Log

Running log of audits performed on the custom OVN integration in this CloudStack
fork. Each audit file lists every bug surfaced, its severity, the commit that
fixed it (or a `WONTFIX` / `DEFERRED` marker), and the production verification
proving the fix landed.

## Purpose

Without this log, every fresh audit dispatch keeps re-flagging bugs that were
already fixed in an earlier session. The conductor (or a subagent) must read
the relevant audit file BEFORE running an open-ended "find all the bugs" pass
and scope its analysis to either:

- gaps the prior audit explicitly marked as `OPEN`/`DEFERRED`, OR
- surface area that did not exist at the time of the prior audit
  (newer commits, newer modules).

## Conventions

- One file per audit pass: `YYYY-MM-DD-<topic>.md`.
- Status states: `FIXED`, `OPEN`, `DEFERRED`, `WONTFIX`, `OBSOLETE`.
- Every `FIXED` entry MUST cite the fix commit hash + production verification
  evidence (build md5, smoke-test output, API field returned, etc.).
- Audits are append-only. Never edit historical files; create a new file when
  re-auditing the same surface.
- en-US for all content (project policy).

## Index

| Date | File | Scope | Bugs found | Bugs fixed | Bugs open |
|---|---|---|---|---|---|
| 2026-05-09 | `2026-05-09-ovn-fork-audit.md` | OVN fork DB + API + UI inconsistencies + LB selection_fields + LB update state-machine + vDPA user VM guard + VF pool concurrent allocation race | 12 | 12 | 0 |
| 2026-05-10 | `2026-05-10-bug-13-configkey-leak.md` | Bug 13 — `ovn.requested_chassis` + `ovn.ha_chassis_priority` ZONE-scope ConfigKeys left set from Phase B verification audit + never reverted; every new LSP carried `options:requested-chassis="norbert=42"`; HIGH severity; config-only revert (no source change, no rebuild); verified via Stage 2 rebuild (20 VMs, 0 contaminated LSPs) | 1 | 1 | 0 |
| 2026-05-10 | `2026-05-10-bug-14-iface-id-prefix.md` | Bug 14 — `OvnVifDriver` emits raw NIC UUID as `external_ids:iface-id` instead of `lsp-<uuid>`; TAP-tier-only regression from `d85d27f126`; promised `OvnNicTunableApplier` post-plug stamp never implemented; 6 TAP LSPs stuck `up=false` (Port_Binding never claimed); HIGH severity; manual remediation via `ovs-vsctl set Interface … external_ids:iface-id=lsp-<uuid>` on fluffy + trevor (6/6 LSPs `up=true`); source patch in `applyPostPlugTunables` mirrors vDPA + VF passthrough drivers. **Verification gap appended 2026-05-10**: `applyPostPlugTunables` had zero callers until commit `8e9b913cb1`; cold-start production fix worked only via manual stamp applied before JAR was built — see audit file "Verification gap" section. | 1 | 1 | 0 |
| 2026-05-10 | `2026-05-10-bug-14b-and-15-migration.md` | Bug 14b — live-migration destination OVN tap never runs `OvnVifDriver.plug()` → post-plug stamp never fires → dest tap has raw UUID iface-id → Port_Binding not claimed after migration; HIGH; Layer A fix `ff59e27753`; Layer B agent fix `0d91ba8a3f`; Layer B mgmt fix `d34f9fe190` (`dispatchPostMigrateOvnStamp` in `VirtualMachineManagerImpl`). Bug 15 Layer A — vDPA destination VF never allocated during migration → `No such file or directory`; MED; Layer A fix `ff59e27753`; Layer B agent fix `f03d7cfbfa`; Layer B mgmt fix `b540cbd183` (`buildMigrateCommand` vdpa mapping plumb); Layer C fix `3bfbf9e596` (rollback VF release). Shared root cause: legacy `getVifDriver(TrafficType)` call in `LibvirtPrepareForMigrationCommandWrapper:80`. | 2 | 6 (Layers A+B+C all layers including mgmt wiring) | 0 |
| 2026-05-10 | `2026-05-10-hw-offload-audit-correction.md` | Corrective audit revoking 3 spurious findings from an earlier HW-offload pass — C1 `tc-policy=none` flagged as P0 blocker (FALSE_POSITIVE: `none` is OVS default, dual-path SW+HW, NOT a block-mode); C2 `doca-init=false` flagged as P1 missed optimization (WONTFIX: operator policy keeps DOCA OFF); C3 14 raw-UUID `iface-id` interfaces flagged as Bug-14 regression candidates (FALSE_POSITIVE: every raw-UUID interface lives on `cloud0`/`br-bond` non-OVN bridges, 0 on `br-int` cluster-wide). 71% effective offload of offloadable flows; SW residue is architectural (recirc multi-table, mcast, dynamic tunnel encap, drop actions). No source/config changes. | 3 | 0 (all FALSE_POSITIVE/WONTFIX) | 0 |
| 2026-05-10 | `2026-05-10-bug-16-17-vdpa-tc-race.md` | Bug 16 — vDPA-tier VMs never receive DHCP; OVN table 73 DHCP exception rule matches 0 packets; cloud-init breaks. Bug 17 — TCP from vDPA VMs never establishes; SYN-ACK storm + guest RST; only ICMP works. Shared root cause: `OvnVdpaVifDriver.attachRepresentorToBrInt()` stamped `iface-status=active` at plug() time before vhost-vdpa queue negotiation completed → mlx5 TC offload partial install (chain 0 has `ct zone N nat pipe + goto chain 1`, chain 1 empty) → all TCP/UDP HW-dropped. ICMP escapes because chain 0 only matches TCP+UDP. Both HIGH. Fix `bc76f2a8fc` defers active stamp to `LibvirtComputingResource.applyOvnPostPlugTunables()` post-VM-running hook via new `applyVdpaPostPlugTunables()`, mirroring proven Bug-14 TAP pattern; rep name cached on `NicTO.setVfRepName()` during plug. Companion `e10dd14676` drops 4 pre-existing unused imports blocking `package` build. JAR md5 `312819d405eefebe673bb9a89f3df13f` deployed norbert + fluffy. Verified same-host on fluffy via 2 fresh vDPA VMs (`perf-vdpa-dst` + `perf-vdpa-2`): DHCP lease 86400s on both, ICMP 5/5 0% loss, iperf3 TCP 3.28 Gbps sustained 15s 4 streams. Cross-host verification DEFERRED — norbert kernel `mlx5_vdpa` deadlock (concurrent `vdpa dev add`/`del` on same VF, D-state, unreapable; separate kernel issue, NOT introduced by patch). Bug 18 (tier-vdpa LS has no router-type LSP, `lrp-fa50740c` `peer=[]`) OPEN, separate audit pending. | 2 | 2 (Bug 16 + 17 FIXED) | 1 (Bug 18 routing) |
| 2026-05-10 | `2026-05-10-bug-18-ls-lr-peer-backfill.md` | Bug 18 — all 3 tiers of VPC `test20` had LRP present in OVN NB but zero `type=router` peer LSPs on the tier LS; VMs could not ARP for tier gateway; inter-tier routing broken. Root cause H2: `ensureTierBoundToVpcLr` short-circuited on the `existing != null` path after confirming the LRP row without ever checking the peer LSP existed. Fix `cd1d7ad5f0` adds `OvnNbClient.ensureRouterPeerLsp()` (idempotent insert of missing `type=router` LSP with `options:router-port`) called from the reconcile short-circuit. Build md5 `c4b4373fd56aac1b613adc4eef81f315`; deployed all 3 controls (voldemort/bellatrix/barty) via rolling restart. Reconcile triggered via `restartNetwork` (errorcode 530 in cleanup phase, but impl ran first). All 3 `rsp-<tierUUID>` LSPs backfilled; ARP from `perf-vdpa-dst` 3/3 replies; ICMP 3/3 0% loss; gateway MAC `02:01:01:61:01:01` confirmed. | 1 | 1 (Bug 18 FIXED) | 0 |
| 2026-05-10 | `2026-05-10-bug-19-staticnat-idempotency.md` | Bug 19 — `OvnStaticNatService.addStaticNat` unconditionally persisted a new `ovn_logical_id_map` row on every call; second call (e.g. `restartNetwork cleanup=false` → `reprogramNetworkRules` → `applyStaticNats`) hit unique constraint `uc_ovn_lim_cs`, threw `SQLIntegrityConstraintViolationException` → propagated as INTERNAL_ERROR (errorcode 530). Fix `ed255c6713` adds an idempotency guard (`handleExistingMapping` private helper) mirroring the `SOURCE_NAT` pattern in `OvnNetworkElement.applyIps`. Fat-JAR md5 `5163fe2bbbd4f59266babe4a863a86cc`; deployed all 3 controls via rolling restart. All 3 tiers (tap-vdpa/tap-vf/tap-tap) return `success: true`; no 530; management log shows `restartRequired=false` with no exceptions. | 1 | 1 (Bug 19 FIXED) | 0 |
| 2026-05-11 | `2026-05-11-bug-20-ovs-setup-frr-restart-hang.md` | Bug 20 — `/usr/local/bin/ovs-setup.sh` line 290 issues `systemctl try-restart frr` from inside `ovs-setup.service` (TimeoutStartSec=600) which declares `Before=frr.service`. Reverse dependency edge + waiting on frr restart job blocks systemctl client ~8 min 13 s (norbert 03:56:24 → 04:04:37 on 2026-05-11) until externally killed. Manual `kill -9 354739` unblocks. MEDIUM severity — every host reboot or manual ovs-setup restart delays follow-up units up to 10 min. Fix surface enumerated (remove inline restart + drive via systemd dep graph, or add `--no-block`, or drop `Before=frr.service` edge). No source change yet. | 1 | 0 | 1 (Bug 20 OPEN) |
| 2026-05-11 | `2026-05-11-bug-21-trevor-agent-silent-failure.md` | Bug 21 — `cloudstack-agent` on `trevor` stayed in `failed` state for ~8 h with zero alerting; cleanup blocked by stale `/var/run/ovn/ovn-controller.pid` referencing dead PID 2989309. `ovn-host.service` does not declare `PIDFile=` correctly. Resolution: `pkill -9 ovn-controller && rm /var/run/ovn/ovn-controller.pid && systemctl reset-failed && systemctl start`. MEDIUM severity. Fix surface: `PIDFile=` + `ExecStartPre=-/bin/rm -f` in ovn-host.service + Prometheus alert rule for `cloudstack-agent` failed state. No source change yet. | 1 | 0 | 1 (Bug 21 OPEN) |
| 2026-05-11 | `2026-05-11-bug-22-vr-tier-nic-dropped.md` | Bug 22 — Multi-tier VPC VR (`r-1164-VM`, VPC `test-20vm-vpc`) booted on norbert with only 3 of 4 NICs in libvirt; missing NIC is the non-HW-offload tier `tap-tap` (MAC `02:04:02:55:00:13` IP `10.97.3.96`). Symptom is downstream of upstream defect — `_agentMgr.send(destHostId, cmds)` in `VirtualMachineManagerImpl.orchestrateStart` line 1563 sends `StartCommand` + per-tier post-start commands (PlugNicCommand for non-HW-offload tiers + 3x SetupGuestNetworkCommand + AggregationControl + IP-assoc + ACL + monitor + LB + StaticRoute + VPN ≈ 12-15 cmds) as one Request with `OnError.Stop`. Agent processed StartCommand only (804 ms); subsequent commands stalled / agent SIGKILLed at 04:42:39; mgmt Request timed out at 04:42:39,612 → `AgentUnavailableException Host 10`; `vmGuru.finalizeStart` at line 1580 never reached. `ClusteredVirtualMachineManagerImpl` later flipped DB Starting -> Running based on power-on report at 04:43:28,275 — VR appears healthy in API but libvirt has 3 interfaces, OVN NB has 4 LSPs. HIGH severity (every multi-tier VPC VR with mix of HW-offload + non-HW-offload tiers at risk; user-visible only when guest VMs in dropped tier fail DHCP). Patch surface enumerated: split batch into StartCommand-only sub-batch + retryable post-start sub-batch; OR reconcile-by-power-report cross-check between `nics` rowcount and libvirt `<interface>` count; OR persistent `op_vr_pending_plug` table for replay. No fix proposed; audit-only per user request. | 1 | 0 | 1 (Bug 22 OPEN) |
| 2026-05-11 | `2026-05-11-bug-23-vdpa-iface-lookup.md` | Bug 23 — `LibvirtDomainXMLParser.parseDomainXML` iterated `<interface>` elements and matched only on bridge/network/ethernet/vhostuser/hostdev types, silently producing a phantom `InterfaceDef` (mac=null, netType=null) for every `<interface type='vdpa'>` element. Every MAC-based libvirt lookup downstream (SetupGuestNetworkCommandWrapper, hot-plug correlation, OVN tier resolution, post-migrate stamp, NIC delete) skipped the phantom row via their null-mac guards, failed to locate the real vDPA NIC, and returned `Can not find nic with mac <vdpa-mac> for VM <vr>`. The StartCommand batch aborted via `OnError.Stop`, leaving the non-HW-offload tier PlugNicCommand unprocessed — the **upstream root cause of Bug 22**. HIGH severity. Fix `71dcc1a633` adds a `vdpa` branch calling `InterfaceDef.defVdpaNet(vhostDevPath, mac, null)` mirroring `OvnVdpaVifDriver.plug` emission, plus a defensive `else` that logs unrecognized types at WARN and skips the add (protects future libvirt types from the same phantom-row class). JUnit suite `LibvirtDomainXMLParserVdpaTest` (4 tests) exercises parser against canonical VR libvirt XML containing bridge + vDPA + hostdev. JAR md5 `36dc5a92595ecb2e001ad590666a35e2` deployed all 6 data nodes (aragog/norbert/fluffy/nagini/scabbers/trevor). Smoke-test verified on r-1165-VM (UUID `97cebb55-a68f-4b2e-8545-8f0cdf5f9087`) via `cmk startRouter`: state Running on aragog, `virsh dumpxml` shows 4 interfaces including the previously-dropped tier-tap `02:04:02:55:00:14`, agent log shows `SetupGuestNetwork: found interface type=vdpa mac=02:04:02:53:00:17` and no `Can not find nic` ERROR. | 1 | 1 (Bug 23 FIXED) | 0 |
| 2026-05-11 | `2026-05-11-bug-24-vr-tier-tap-bridge-mis-assignment.md` | Bug 24 — After Bug 23 fix, tier-tap NIC appears in libvirt XML but `<source bridge='br-bond'/>` (wrong, should be `br-int`) AND OVS `external_ids:iface-id` is raw NIC UUID (Bug 14 lsp-prefix pattern relapsed on VR-side only). r-1165 tier-tap MAC `02:04:02:55:00:14` on `vnet100` lives on br-bond; OVN br-int flows programmed for that MAC but vnet100 traffic never enters OVN pipeline. VR cannot ping tier-tap gateway 10.97.3.1 or any tier-tap VM. HIGH severity — every multi-tier VPC VR with non-HW-offload tier loses that tier connectivity. Two-layer defect: (1) `LibvirtPlugNicCommandWrapper` bridge resolution picks br-bond instead of br-int; (2) `applyPostPlugTunables` not extended to VR Guest tier NICs. Manual remediation: `ovs-vsctl del-port br-bond <vnet>; add-port br-int <vnet>; set Interface <vnet> external_ids:iface-id=lsp-<uuid>`. No source change yet. | 1 | 0 | 1 (Bug 24 OPEN) |
| 2026-05-11 | `2026-05-11-bug-25-old-vm-unreachable-after-agent-restart.md` | Bug 25 — Rolling `cloudstack-agent` restart across 6 data nodes (executed 2026-05-11 to deploy Bug 23 JAR) caused all 20 pre-existing `test20-*` VMs in VPC `test-20vm-vpc` to lose east-west connectivity (VR-side ping 100% loss). 9 perf-* VMs created after the restart retained connectivity. Root cause: `applyPostPlugTunables` post-plug stamp only fires on fresh plug; never re-runs on agent restart. OVS Interface rows persist with `iface-id=lsp-<uuid>` + `iface-status=active` + `ovn-installed-ts=<old-epoch>` but ovn-controller does not re-flip `ovn-installed=true` on re-sync. Same class as Bug 14 verification gap. MEDIUM severity. Fix surface: agent-startup reconcile pass walking running VMs + reapplying stamp idempotently. Manual remediation: per-host loop reapplying `ovn-installed=true` + `systemctl restart ovn-controller`. No source change yet. | 1 | 0 | 1 (Bug 25 OPEN) |
| 2026-05-11 | `2026-05-11-bug-26-post-migrate-ovn-installed-flag-missing.md` | Bug 26 — Live-migration of perf-vdpa-2 (fluffy → norbert via `cmk migrate`) succeeded at DB+libvirt layer but destination OVS Interface `dx6p1vf5` (mac `02:04:02:53:00:14`, iface-id `lsp-92a30b54-...`) stamped `iface-status=active` + `ovn-installed-ts="1778478045957"` WITHOUT `ovn-installed="true"` flag → VR ping post-migrate 100% loss (5/5 packets). HIGH severity — Bug 14b partial regression. Post-migrate stamp dispatch from `VirtualMachineManagerImpl.dispatchPostMigrateOvnStamp` (commit `d34f9fe190`) writes iface-id + iface-status but ovn-controller does not re-trigger bind on already-claimed iface-id → only updates ts. Fix surface: agent-side post-migrate stamp adds `external_ids:ovn-installed=true` to the `ovs-vsctl set Interface` argv. Manual remediation: `ovs-vsctl set Interface <rep> external_ids:ovn-installed=true`. Bug 14b verification on 2026-05-10 insufficient — flag presence not asserted in test acceptance. No source change yet. | 1 | 0 | 1 (Bug 26 OPEN) |
